---
name: sbx
description: Host-side commands for this sandbox (open Zed, open a shell, recover commits, publish a port, stop, remove). Use whenever the user has to act on the host, for example to open a dev server in their browser, open the project in Zed, or fetch the work done here.
allowed-tools: Bash(bash:*)
---

Show the block below to the user exactly as it is, inside a code block. Do not edit, reorder or complete any command: they are pasted on the host as they are. If the user asked about something specific (a port, Zed, recovering work), say in one sentence which lines answer it.

!`bash ~/.claude/skills/sbx/host-commands.sh`
